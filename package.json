{
    "name": "@dialogintelligens/scripts",
    "scripts": {
        "build": "esbuild src/main.ts --bundle --minify --outfile=dist/universal-chatbot.js --platform=browser --target=es2018",
        "release": "esbuild src/main.ts --bundle --minify --outfile=universal-chatbot.js --platform=browser --target=es2018",
        "format": "prettier --write src"
    },
    "devDependencies": {
        "esbuild": "^0.27.0",
        "prettier": "^3.6.2",
        "typescript": "^5.0.0"
    },
    "engines": {
        "node": "25.1.0"
    },
    "packageManager": "pnpm@9.0.0"
}